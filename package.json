{
  "name": "kai-research-log",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "db:local:init": "wrangler d1 execute kai-research-db --local --file=./db/schema.sql",
    "db:remote:init": "wrangler d1 execute kai-research-db --remote --file=./db/schema.sql",
    "db:seed:generate": "node scripts/generate-d1-seed.mjs",
    "db:local:seed": "npm run db:seed:generate && wrangler d1 execute kai-research-db --local --file=./db/seed.sql",
    "db:remote:seed": "npm run db:seed:generate && wrangler d1 execute kai-research-db --remote --file=./db/seed.sql",
    "db:local:clear-tasks": "wrangler d1 execute kai-research-db --local --file=./db/clear-tasks.sql",
    "db:remote:clear-tasks": "wrangler d1 execute kai-research-db --remote --file=./db/clear-tasks.sql",
    "journal:validate": "node scripts/validate-journal-inbox.mjs",
    "journal:dry-run": "node scripts/generate-journal-drafts.mjs --dry-run",
    "journal:generate": "node scripts/generate-journal-drafts.mjs",
    "journal:test": "node --test tests/journal-pipeline.test.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.3",
    "js-yaml": "^4.2.0",
    "sharp": "^0.34.3"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "@cloudflare/workers-types": "^5.20260717.1",
    "wrangler": "^4.111.0"
  }
}
